import ch.so.agi.gretl.tasks.*
import ch.so.agi.gretl.api.TransferSet

apply plugin: 'ch.so.agi.gretl'

defaultTasks 'csvimport'

def schemaNameEdit = "gesa_tigermueckenfundstellen_v1"

tasks.register('deleteCsvTableRows', SqlExecutor){
    database = [dbUriEdit, dbUserEdit, dbPwdEdit]
    sqlParameters = [dbSchema:schemaNameEdit]
    sqlFiles = files(
        'delete_csv.sql'
    )
}

tasks.register('fixCsvHeader') {
    dependsOn deleteCsvTableRows
    doLast {
        def file = file("upload/Tigermuecken_SO_W38.csv")
        def lines = file.readLines('UTF-8')
        lines[0] = lines[0].replace('UID', 'auid')
        file.write(lines.join('\n'), 'UTF-8')
    }
}

tasks.register('csvimport',  CsvImport) {
    dependsOn "fixCsvHeader"
    database = [dbUriEdit, dbUserEdit, dbPwdEdit]
    schemaName = schemaNameEdit
    tableName = "csv_import"
    firstLineIsHeader = true
    valueSeparator = ';'
    dataFile = file("upload/Tigermuecken_SO_W38.csv")
}

/*
tasks.register('transferData', Db2Db) {
    dependsOn "csvimport"
    sourceDb = [dbUriEdit, dbUserEdit, dbPwdEdit]
    targetDb = [dbUriPub, dbUserPub, dbPwdPub]
    transferSets = [
            new TransferSet('gesa_tigermueckenfundstellen_punkt.sql', 'gesa_tigermueckenfundstellen_pub_v1', true),
            new TransferSet('gesa_tigermueckenfundstellen_polygon.sql', 'gesa_tigermueckenfundstellen_pub_v1', true)
    ];
}
*/