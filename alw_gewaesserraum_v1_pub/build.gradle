import ch.so.agi.gretl.tasks.*
import ch.so.agi.gretl.api.TransferSet

apply plugin: 'ch.so.agi.gretl'

defaultTasks 'transferArpStatpopStatentEdit'

task transferAlwGewaesserraumPub(type: Db2Db) {
    sourceDb = [dbUriEdit, dbUserEdit, dbPwdEdit]
    targetDb = [dbUriPub, dbUriPub, dbUriPub]
    transferSets = [
            new TransferSet('alw_gewaesserraum_v1.sql', 'alw_gewaesserraum_v1.gewaesserraum', true)
    ];
    
}

