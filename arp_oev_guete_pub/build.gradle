import ch.so.agi.gretl.steps.*

defaultTasks 'transferArpOevGuete'

task transferArpOevGuete(type: Db2DbTask){
    sourceDb = [sourceDbUrl, sourceDbUser, sourceDbPass]
    targetDb = [targetDbUrl, targetDbUser, targetDbPass]
    transferSets = [
            new TransferSet('arp_oev_guete_pub_oevgueteklasse.sql', 'arp_oev_guete_pub.oevgueteklasse', true)
    ];
}
