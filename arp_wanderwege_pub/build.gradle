import ch.so.agi.gretl.steps.*

defaultTasks 'transferArpWanderwege'

task transferArpWanderwege(type: Db2DbTask){
    sourceDb = [sourceDbUrl, sourceDbUser, sourceDbPass]
    targetDb = [targetDbUrl, targetDbUser, targetDbPass]
    transferSets = [
            new TransferSet('arp_wanderwege_pub_wanderweg.sql', 'arp_wanderwege_pub.wanderweg', true)
    ];
}
